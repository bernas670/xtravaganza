using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fmod_AmbientSound_Start : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SendMessage("Play");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
